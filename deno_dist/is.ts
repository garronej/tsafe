import { is } from "./assert.ts";

/**
 * @deprecated: Prefer importing using `import { assert, is } from "tsafe/assert"`
 * or `import { assert, is } from "tsafe"`
 *
 * https://docs.tsafe.dev/is
 * */
export { is };
